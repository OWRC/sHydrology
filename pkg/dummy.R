###########################################################################################
## Dummy functions used in other deployments
###########################################################################################
drawCarea <- function(lat, lng) {
  # doing nothing
}

getMeteo <- function(lat, lng) {
  return(NULL)
}